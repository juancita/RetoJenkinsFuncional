/**
Realiza una llamada a devops a la rama mmaster
*/
@Library('devops@master') _
/*obtenga los parametros de las librerías compartidas del 
repositorio “devops” cuando se ejecute el pipeline en Jenkins */
pipelineGeneral(scmUrl: 'https://github.com/JoseCorreaV/devops.git')
